/target
/dist
# Permission schemas regenerated by tauri-build on every build.
/app/gen/schemas
# `cargo tauri icon` writes a set per platform. This app is desktop-only, so
# these two have no target to be packaged into; they regenerate from
# app/app-icon.png the moment there is one.
/app/icons/android
/app/icons/ios
