[package]
name = "treestamp"
version = "0.1.0"
edition = "2024"
description = "Bakes a build's version, commit and time into a binary"
license = "MIT OR Apache-2.0"
publish = false

# No dependencies, on purpose: this is both a build-dependency and a
# dependency of every app that uses it, so anything it pulls in is paid for
# twice and once before the build proper starts.
[dependencies]
